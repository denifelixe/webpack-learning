// CSS Loader
// import css from './main.css';
// or you can use
// import './main.css';

// Sass Loader
import './main.scss';
import './second.scss';

// import notifyFromNotification from './Notification'; 
// // We use relative modules by using './'
// // We can also import things that we've been installed to node_modules, example 
// // import notify from 'my-package'; 
// // In this case we wouldn't use the relative path './'

// notifyFromNotification('Here is my message');

// Babel ES2015 into backwards compatible compiler
// class Form {
//     constructor() {
//         let numbers = [1, 2, 3].map((n) => n + 1);

//         console.log(numbers);
//     }
// }

// new Form();